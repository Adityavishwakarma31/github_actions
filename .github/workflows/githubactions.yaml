name: GitHub Actions Demo
run-name: For-practice
on:
  push:
    paths:
      - '**.md'
jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - uses: docker://alpine:3.8
      - run: sudo docker ps
        
      
